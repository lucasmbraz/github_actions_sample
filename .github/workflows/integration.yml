name: Integration tests

on:
  pull_request:
    branches:
      - main
    types:
      - labeled

jobs:
  integration_tests:
    name: Simulate running integration tests
    runs-on: ubuntu-latest
    steps:
      - name: Fail randomly
        run: exit $((RANDOM % 2))